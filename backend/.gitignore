node_modules/
.env
dist/
*.log
.DS_Store

# Keep uploads/ directory but ignore its contents (disk storage is local dev only)
uploads/*
!uploads/.gitkeep

# GCP + Firebase service account credentials — NEVER commit these
gcp-service-account.json
firebase-service-account.json
*-service-account.json
*serviceAccount*.json