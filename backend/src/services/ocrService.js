/**
 * OCR service — extracts license-plate text from evidence photos using
 * Google Cloud Vision, then compares the result against OCR_CONFIDENCE_THRESHOLD
 * to decide whether manual review is required.
 * Stubbed for Sprint 1; implementation lands in a later sprint.
 */
// const vision = require('@google-cloud/vision');

module.exports = {};
