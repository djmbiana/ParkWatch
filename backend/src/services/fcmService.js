/**
 * FCM service — sends push notifications via Firebase Cloud Messaging and
 * records each send in NOTIFICATION_LOG.
 * Stubbed for Sprint 1; implementation lands in a later sprint.
 */
// const { getMessaging } = require('../config/firebase');

module.exports = {};
