/**
 * Storage service — uploads evidence photos to Google Cloud Storage
 * (GCS_BUCKET_NAME) and returns their object URLs.
 * Stubbed for Sprint 1; implementation lands in a later sprint.
 */
// const { Storage } = require('@google-cloud/storage');

module.exports = {};
