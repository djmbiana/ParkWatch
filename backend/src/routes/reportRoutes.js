const express = require('express');

const router = express.Router();

// Violation-report endpoints (submit, verify, dispatch, resolve) — implemented in a later sprint.
// Example:
//   const reportController = require('../controllers/reportController');
//   router.post('/', authenticate, upload.single('photo'), reportController.create);

module.exports = router;
