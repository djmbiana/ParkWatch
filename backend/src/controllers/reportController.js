/**
 * Report controller — violation-report lifecycle: submit, verify, acknowledge,
 * dispatch, resolve, reject (VIOLATION_REPORTS table).
 * Stubbed for Sprint 1; handlers land in a later sprint.
 */
module.exports = {};
